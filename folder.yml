---
   - hosts: localhost
     #remote_user: root
     #sudo: yes
     become: yes
     tasks:
     - name: Folder creation
       file:
        name: /home/ubuntu/Linux
        state: directory
        mode: 0755
        owner: ubuntu
        group: ubuntu
