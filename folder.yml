---
   - hosts: localhost
     #remote_user: root
     become: yes
     #sudo: yes
     tasks:
     - name: Create Folder
       file:
        path: /home/ubuntu/devops-world
        state: directory
