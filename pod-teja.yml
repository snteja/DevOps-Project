apiVersion: v1
kind: Pod
metadata:
  name: project-pod
  labels:
    app: project
spec:
  containers:
    - name: project-container
      image: sainava225/devops-app
      ports:
        - containerPort: 8080
